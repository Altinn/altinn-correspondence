meta {
  name: Search (as end user)
  type: http
  seq: 4
}

get {
  url: {{correspondence_base_url}}/correspondence/api/v1/correspondence
  body: none
  auth: bearer
}

auth:bearer {
  token: {{recipient_altinn_token}}
}
