meta {
  name: Search (as end user)
  type: http
  seq: 4
}

get {
  url: {{correspondence_base_url}}/correspondence/api/v1/correspondence?role=Recipient&resourceId={{resource_id}}
  body: none
  auth: bearer
}

params:query {
  role: Recipient
  resourceId: {{resource_id}}
}

auth:bearer {
  token: {{recipient_altinn_token}}
}
