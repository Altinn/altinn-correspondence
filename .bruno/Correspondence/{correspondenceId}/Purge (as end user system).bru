meta {
  name: Purge (as end user system)
  type: http
  seq: 6
}

post {
  url: {{correspondence_base_url}}/correspondence/api/v1/correspondence/{{correspondenceId}}/actions/purge
  body: json
  auth: bearer
}

auth:bearer {
  token: {{recipient_altinn_token}}
}

body:json {
  {
    "recipient": "urn:altinn:organization:identifier-no:{{recipient_orgnumber}}"
  }
}
