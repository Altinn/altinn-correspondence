meta {
  name: Download attachment (as end user system)
  type: http
  seq: 5
}

get {
  url: {{correspondence_base_url}}/correspondence/api/v1/correspondence/{{correspondenceId}}/attachments/{{attachmentId}}/download
  body: none
  auth: bearer
}

auth:bearer {
  token: {{recipient_altinn_token}}
}
