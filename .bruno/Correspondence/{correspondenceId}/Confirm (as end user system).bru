meta {
  name: Confirm (as end user system)
  type: http
  seq: 3
}

post {
  url: {{correspondence_base_url}}/correspondence/api/v1/correspondence/{{correspondenceId}}/confirm
  body: json
  auth: bearer
}

auth:bearer {
  token: {{recipient_altinn_token}}
}
