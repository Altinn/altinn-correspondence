meta {
  name: Initialize batch attachment
  type: http
  seq: 1
}

post {
  url: {{correspondence_base_url}}/correspondence/api/v1/attachment
  body: json
  auth: bearer
}

auth:bearer {
  token: {{serviceowner_altinn_token}}
}

body:json {
  {
    "resourceId": "{{resource_id}}",
    "fileName": "test-document.pdf",
    "sendersReference": "746011"
  }
}

script:post-response {
  const responseData = res.body;
  bru.setVar("attachment_id", responseData);
}
