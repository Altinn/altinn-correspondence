meta {
  name: Delete subscription
  type: http
  seq: 4
}

delete {
  url: {{platform_base_url}}/events/api/v1/subscriptions/{{event_subscription_id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{serviceowner_altinn_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
